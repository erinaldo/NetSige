SELECT
    ITEM.ID,
    ITEM.STATUSID,
    ITEM.CREATIONDATE,
    ITEM.PHOTOPATH,
    ITEM.NAME,
    ITEM.ITEMUNITID,
    ITEM.ITEMGROUPID,
    ITEM.INTERNALCODE,
    ITEM.MANUFACTURER,
    ITEM.MANUFACTURERCODE,
    ITEM.NOTE,
    ITEM.USERID
FROM
    ITEM
WHERE
    ITEM.ID = @ID;