SELECT
    QUOTATION.ID,
    QUOTATION.STATUSID,
    QUOTATION.CREATIONDATE,
    QUOTATION.COSTID,
    QUOTATION.OFFERTYPEID,
    QUOTATION.NOTE,
    QUOTATION.USERID
FROM QUOTATION
WHERE QUOTATION.ID = @ID;