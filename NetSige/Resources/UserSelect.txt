SELECT USER.ID,
    USER.STATUSID,
    USER.CREATIONDATE,
    USER.USERNAME,
    USER.PASSWORD,
    USER.NAME,
    USER.EMAIL,
    USER.PHONE,
    USER.USERID
FROM USER
JOIN USER AS USER2 ON USER.ID = USER2.ID
WHERE USER.ID = @ID;